"""Nebula gameplay domains.

One subpackage per domain. Each owns its own service, definitions and, where
it needs one, its agent graph. Battle and party land here as `battle/` and
`party/` without touching anything else.
"""
